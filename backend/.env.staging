# Staging Environment Configuration
# This file contains settings for staging/pre-production environment

# Database Configuration
# PostgreSQL is recommended for staging to match production
# Replace with your actual staging database credentials
DATABASE_URL=postgresql://neo_alexandria_user:CHANGE_ME_PASSWORD@staging-db-host:5432/neo_alexandria_staging

# Test Database (optional - use PostgreSQL for realistic testing)
# Create a separate test database for staging environment tests
TEST_DATABASE_URL=postgresql://neo_alexandria_user:CHANGE_ME_PASSWORD@staging-db-host:5432/neo_alexandria_test

# Environment
ENV=staging

# Quality Control
MIN_QUALITY_THRESHOLD=0.7
BACKUP_FREQUENCY=daily
TIMEZONE=UTC

# Redis Configuration (for Celery and caching)
# Use staging Redis instance
REDIS_HOST=staging-redis-host
REDIS_PORT=6379
REDIS_CACHE_DB=2
CELERY_BROKER_URL=redis://staging-redis-host:6379/0
CELERY_RESULT_BACKEND=redis://staging-redis-host:6379/1

# Vector Embedding Configuration
EMBEDDING_MODEL_NAME=nomic-ai/nomic-embed-text-v1
DEFAULT_HYBRID_SEARCH_WEIGHT=0.5
EMBEDDING_CACHE_SIZE=1000

# Graph Configuration
DEFAULT_GRAPH_NEIGHBORS=7
GRAPH_OVERVIEW_MAX_EDGES=50
GRAPH_WEIGHT_VECTOR=0.6
GRAPH_WEIGHT_TAGS=0.3
GRAPH_WEIGHT_CLASSIFICATION=0.1
GRAPH_VECTOR_MIN_SIM_THRESHOLD=0.85

# Recommendation Configuration
RECOMMENDATION_PROFILE_SIZE=50
RECOMMENDATION_KEYWORD_COUNT=5
RECOMMENDATION_CANDIDATES_PER_KEYWORD=10
SEARCH_PROVIDER=ddgs
SEARCH_TIMEOUT=10

# Security Notes:
# - Never commit actual credentials to version control
# - Use environment-specific secrets management (AWS Secrets Manager, HashiCorp Vault, etc.)
# - Rotate credentials regularly
# - Use SSL/TLS for database connections in production-like environments
